name: V4 Portfolio OS Sync

on:
  workflow_dispatch:
  schedule:
    - cron: "17 */6 * * *"
  push:
    branches: [main]
    paths:
      - "scripts/update_profile.py"
      - ".github/workflows/v2-3-live-profile.yml"

permissions:
  contents: write
  actions: read
  pull-requests: read
  issues: read

concurrency:
  group: profile-v4-portfolio-os
  cancel-in-progress: true

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout profile
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Generate V4 portfolio operating system
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python scripts/update_profile.py

      - name: Commit only when public state changed
        run: |
          if git diff --quiet -- README.md assets/live-command.svg assets/portfolio-graph.svg; then
            echo "No V4 portfolio state changes."
            exit 0
          fi
          git config user.name "joe-command-bot"
          git config user.email "actions@users.noreply.github.com"
          git add README.md assets/live-command.svg assets/portfolio-graph.svg
          git commit -m "chore(profile): sync v4 portfolio os"
          git push
